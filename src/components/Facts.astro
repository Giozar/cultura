---
import Icon from "./Icon.astro";
const {section, image} = Astro.props;
---

<section class="h-80">
    <button>
        <Icon image=`${image}.png` />
        <h3 class="text-center font-semibold text-2xl">{section}</h3>
    </button>
</section>
